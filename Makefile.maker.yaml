# Configuration file for <https://github.com/sapcc/go-makefile-maker>

golangciLint:
  createConfig: true

githubWorkflow:
  ci:
    enabled: true
    ignorePaths: [ "**.md" ] # all Markdown files
  license:
    enabled: true
  securityChecks:
    enabled: true
  spellCheck:
    enabled: true

renovate:
  enabled: true
  assignees:
    - majewsky
    - SuperSandro2000
